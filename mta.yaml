ID: training
_schema-version: '2.1'
version: 0.0.1
modules:
 - name: backend
   type: nodejs
   path: backend
   provides:
    - name: backend_api
      properties:
         url: ${default-url}
   requires:
       - name: postgre-db
resources:
  - name: postgre-db
    type: org.cloudfoundry.existing-service